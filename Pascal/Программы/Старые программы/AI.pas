Program AI;
var a, b, x, i, o :integer;
begin
  o :=x div 2 + x;
  a := 0;
  b := 1000;
  writeln('Задумайте число от 1 до 1000');
 x := b div 2;
  writeln('Это число ', x,'? есле больше 1 есле, меньше то 0, а есле угадали то задумай новое');
  if o then write('') ;
writeln(i);
end.